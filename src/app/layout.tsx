// app/layout.tsx
import React from "react";
import Header from "./components/header";
import Footer from "./components/footer";
import "./globals.css"; // Import global styles
import { Metadata } from "next";

export const metadata: Metadata = {
  title: { //if you want to set a default title and also have a template for other pages to follow
    default: "Routing Demo",
    template: "%s | Routing Demo",
    absolute: "",
  },
  description: "Generated by Next.js",
};

export default function RootLayout({
  children, //it is a special prop that is automatically passed to components that are used as layouts in Next.js. It represents the content that will be rendered within the layout.
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <header
          style={{
            backgroundColor: "lightblue",
            padding: "1rem",
          }}
        >
          <Header />
        </header>
        {children}
        <footer
          style={{
            backgroundColor: "ghostwhite",
            padding: "1rem",
          }}
        >
          <Footer />
        </footer>
      </body>
    </html>
  );
}
